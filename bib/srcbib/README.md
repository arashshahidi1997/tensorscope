# srcbib

Put BibTeX exports here (e.g. from Zotero / BetterBibTeX).

Then merge them into a single repo-local BibTeX file:

```bash
biblio bibtex merge
```

Configure merge behavior in `bib/config/biblio.yml` under `bibtex.merge`.

