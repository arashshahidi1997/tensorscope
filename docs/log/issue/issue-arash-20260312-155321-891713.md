---
title: "issue"
status: open
created: 2026-03-12
updated: 2026-03-12
timestamp: 20260312-155321-891713
tags: [issue]
---

# issue

I am describing a few issues that I observe in the app. The spectrogram. Time axis needs labels. And similar gestures should work on. The time axis. In general for whenever the time axis when whenever we have X axis as time something very long, we should include the options for the time axis like the same that we have for time series view, the time scale selection sub bar and also the drag move click and drag option and also the wheel scroll. To zoom in the scale, scale up or scale down. Same goes for frequency here and also so. Y axis gestures, XS access gestures and also in the. On the plot gestures. So if you click and drag it should select a window for zoom in similar to the box select in the time series and if the drag click and drag is. On the plot, not on the axis on the plot itself. If the drag is horizontal, the selection should be horizontal. It is zoom in on the horizontal so it pans into the that selected box and if the drag is vertical, the pan is likewise vertical, so you zoom in on the Y axis. And these would be similar. Bouquet style gestures that are available for all heat maps. Especially heat maps like spectrograms. Heat maps like spatial maps probably are better without these gestures. Because there there's a it's a relatively small map. Spatial map anyway, and it's not worth to explore with zooms or drags etcetera, so it's better that for that to be stable. But spectrogram is very long and it's required exploration. And the second point that I'm seeing that OK the add transform works. The parameters selection is a bit. Like if it's zero for example at first you cannot delete the window 2nd to to write again, so it has to have something in there. So then I have to put my cursor behind the 1st digit and then write a number and then delete the original number there. So this could be fixed, but this is a minor bug. The main utility that I would like to have is. That transforms by. By the way, PSD the power spectral density that we have as a as a view, which is itself. It's a transform, so it's no different than spectrogram. So power spectral density is. Transform. But maybe we should rethink like what kinds of transforms we have and how to group them together. But my main point is that one should be able to see the signal tensor and the transform tensor, the spectrogram for example. Side by side or top bottom panels as the user decides. So what do you think would be a good UI to enable this? For example in VS code we have this break tabs and columns so you can see two tabs side by side or show them top bottom. Do you think something similar would work here or do you have a better idea? And. Yeah, so that's it for now. Let's try.

## Summary
- 

## Context
- 

## Tasks
- [ ]

## Notes
- 
