<!-- projio:skill-sync — regenerated by projio sync -->

Load the projio skill **questio-session** and follow its instructions.

Call the MCP tool `skill_read` with name="questio-session", read the returned
content carefully, then execute the skill workflow. Pass the user's
input below as the task context.

User input: $ARGUMENTS
